import { CustomdropdownDirective } from './customdropdown.directive';

describe('CustomdropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomdropdownDirective(this.ref);
    expect(directive).toBeTruthy();
  });
});
