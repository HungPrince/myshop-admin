import { SidebardropdownDirective } from './sidebardopdown.directive';

describe('SidebardropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebardropdownDirective();
    expect(directive).toBeTruthy();
  });
});
